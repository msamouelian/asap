apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-pvc
  labels:
    app: vllm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  selector:
    matchLabels:
      app: vllm
