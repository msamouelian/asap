apiVersion: v1
kind: Service
metadata:
  name: vllm-embedding
  labels:
    app: vllm
spec:
  selector:
    app: vllm
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  type: ClusterIP
